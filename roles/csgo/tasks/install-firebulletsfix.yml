---

- name: install firebulletsfix
  include_tasks: install-plugin-github.yml
  vars:
    plugin_id: fbf
    plugin_url: "{{ fbf_download_url }}"
    addons_diretories:
      - plugins
      - gamedata
      - scripting