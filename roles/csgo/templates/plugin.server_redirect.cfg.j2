// This file was auto-generated by glib/convarutils.inc
// ConVars for plugin "server_redirect.smx"


// Commands that invoke servers redirect menu.
// (Note: Map change required for changes to take effect! There's also a 128 character limit per command.)
// -
// Default: "sm_servers;sm_serv;sm_project;sm_list;"
server_redirect_commands "{{ sm_server_redirect_commands | default('sm_servers;sm_serv;sm_project;sm_list;') }}"

// Socket timeout time.
// (Note: Unused if you don't use socket extension)
// -
// Default: "10.0"
// Minimum: "2.000000"
server_redirect_socket_timeout "{{ sm_server_redirect_socket_timeout | default('10.0') }}"

// Show players info when you select server in servers redirect menu.
// (Note: Unused if you don't use socket extension)
// -
// Default: "1"
// Minimum: "0.000000"
// Maximum: "1.000000"
server_redirect_show_player_info "{{ sm_server_redirect_show_player_info | default('1') }}"

// If communication with other servers is timed out, should this be logged?
// (Note: Unused if you don't use socket extension)
// -
// Default: "0"
// Minimum: "0.000000"
// Maximum: "1.000000"
server_redirect_log_if_server_is_unavailable "{{ sm_server_redirect_log_if_server_is_unavailable | default('0') }}"

// Server connect button state:
// (Note: Unused if you don't use socket extension)
// 0 - Always enabled;
// 1 - Disabled if server is password protected, enabled otherwise;
// 2 - Disabled if server is unavailable, enabled otherwise;
// 3 - Enabled only if server is not password protected and avaliable.
// -
// Default: "3"
// Minimum: "0.000000"
// Maximum: "3.000000"
server_redirect_disable_connect_button "{{ sm_server_redirect_disable_connect_button | default('3') }}"

// Will prevent execution of a command if previous execution was less then this seconds before.
// -
// Default: "1.0"
// Minimum: "0.000000"
server_redirect_command_spam_timeout "{{ sm_server_redirect_command_spam_timeout | default('1.0') }}"

// Server advertisement time in seconds (Map change required for this to take effect).
// (Note: set to 0 to disable)
// (Note2: this value should be bigger than server_redirect_socket_timeout by one second or more (Only if you have socket extension installed))
// -
// Default: "60.0"
// Minimum: "0.000000"
server_redirect_advertisement_time "{{ sm_server_redirect_advertisement_time | default('60.0') }}"

// Minimum players required to advertise server in chat.
// (Note: Unused if you don't use socket extension)
// -
// Default: "0"
// Minimum: "0.000000"
server_redirect_advertisement_min_players "{{ sm_server_redirect_advertisement_min_players | default('0') }}"

// Order at which servers gonna be advertised.
// 0 - Order at which servers are defined in config file;
// 1 - Random order.
// -
// Default: "0"
// Minimum: "0.000000"
// Maximum: "1.000000"
server_redirect_advertisement_order "{{ sm_server_redirect_advertisement_order | default('0') }}"

// If set, this current server will also be advertised in chat.
// (Note: Unused if server_redirect_remove_this set to 1!)
// -
// Default: "0"
// Minimum: "0.000000"
// Maximum: "1.000000"
server_redirect_advertise_this "{{ sm_server_redirect_advertise_this | default('0') }}"

// If set, this current server will be removed from servers menu.
// -
// Default: "0"
// Minimum: "0.000000"
// Maximum: "1.000000"
server_redirect_remove_this "{{ sm_server_redirect_remove_this | default('0') }}"

// If set, will show confirmation menu when players will try to connect to some server via servers menu.
// -
// Default: "0"
// Minimum: "0.000000"
// Maximum: "1.000000"
server_redirect_show_confirmation_menu "{{ sm_server_redirect_show_confirmation_menu | default('0') }}"

// If set, will show message to alert players a player has connected to another server.
// -
// Default: "0"
// Minimum: "0.000000"
// Maximum: "1.000000"
server_redirect_show_advertisement_leave "{{ sm_server_redirect_show_advertisement_leave | default('0') }}"

